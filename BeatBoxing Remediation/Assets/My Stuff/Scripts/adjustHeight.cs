using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class adjustHeight : MonoBehaviour
{
    /*
    [SerializeField]
    private static float defaultHeight = 1.8f;
    [SerializeField]
    private static new Camera camera;

    public static void Resize()
    {
        float currentHeadHeight = camera.transform.localPosition.y;
        float percentOffNormal = defaultHeight / currentHeadHeight;
        camera.transform.localScale = Vector3.one * percentOffNormal;
    }
    */
}
